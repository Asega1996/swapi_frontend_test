import { Card } from '@mui/material'
import styled from 'styled-components'

export const CustomCardWrapper = styled(Card)`
    margin: 0 auto;
`

export const CardMediaRegularSize = styled.img`
    max-width: 100%
    margin: 0 auto;
`
